swagger: '2.0'
schemes:
  - https
host: api.zang.io
basePath: /v2
info:
  description: |
    Enabling phones, VoIP, and messaging to be embedded into web, desktop, and mobile software.
  title: Avaya
  version: 'v2'
  x-apisguru-categories:
    - telecom
    - messaging
externalDocs:
  url: 'https://www.zang.io/api/v2'
securityDefinitions:
  basic:
    type: basic
paths:
  '/Accounts/{AccountSid}/Applications/{ApplicationSid}{mediaTypeExtension}':
    delete:
      description: Delete this application.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ApplicationSid
          required: true
          type: string
      responses:
        '204':
          description: Application deleted.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: Get application instance resource.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ApplicationSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/application'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Tries to update the application's properties, and returns the updated
        resource representation if successful. The returned response is identical
        to that returned above when making a GET request.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ApplicationSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/application'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Applications{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of Application resource representations, each representing
        an application within your account. The list includes paging information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/applications'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Creates a new application within your account.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/application'
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/AuthorizedConnectApps/{ConnectAppSid}{mediaTypeExtension}':
    get:
      description: Get the properties of the authorized application.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ConnectAppSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/authApp'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/AuthorizedConnectApps{mediaTypeExtension}':
    get:
      description: |
        Returns a list of Connect App resource representations, each representing a
        Connect App you've authorized to access your account. The list includes
        paging information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/authApps'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/AvailablePhoneNumbers/{IsoCountryCode}/Local{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of local AvailablePhoneNumber resource representations
        that match the specified filters, each representing a phone number tha
        is currently available for provisioning within your account.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - description: ISO 3166-1 alpha-2.
          in: path
          maxLength: 2
          minLength: 2
          name: IsoCountryCode
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/availablePhoneNumbers'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/AvailablePhoneNumbers/{IsoCountryCode}/Mobile{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: 'Returns a list of mobile AvailablePhoneNumber resource representations that match the specified filters, each representing a phone number that is currently available for provisioning within your account.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - description: ISO 3166-1 alpha-2.
          in: path
          maxLength: 2
          minLength: 2
          name: IsoCountryCode
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/phoneMobileNumbers'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/AvailablePhoneNumbers/{IsoCountryCode}/TollFree{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of toll-free AvailablePhoneNumber elements that match the
        specified filters, each representing a phone number that is currently
        available for provisioning within your account. To provision an available
        phone number, POST the number to the IncomingPhoneNumbers resource.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - description: ISO 3166-1 alpha-2.
          in: path
          maxLength: 2
          minLength: 2
          name: IsoCountryCode
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/phoneTollFreeNumbers'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/AvailablePhoneNumbers{mediaTypeExtension}':
    get:
      description: 'Returns a list of all AvailablePhoneNumber subresources for your account by ISO Country. For full information about our phone number support, see our Phone Number CSV (http://www.Avaya.com/resources/rates/international-phone-number-rates.csv).'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/availablePhoneNumbers'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Calls/{CallSid}/Notifications{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of notifications generated for an account. The list includes
        paging information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: CallSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/notifications'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Calls/{CallSid}/Recordings{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of Recording resource representations, each representing a
        recording generated during the course of a phone call.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: CallSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/recordings'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Calls/{CallSid}{mediaTypeExtension}':
    get:
      description: 'Returns the single Call resource identified by {CallSid}.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CallSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/callInstance'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Modify a phone call.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CallSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/callInstance'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Calls{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: 'Returns a list of phone calls made to and from the account identified by {AccountSid}.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/calls'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: 'To make a call, make an HTTP POST request. Initiate a new phone call.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/call'
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Conferences/{ConferenceSid}/Participants/{CallSid}{mediaTypeExtension}':
    delete:
      description: Kick this participant from the conference.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: ConferenceSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CallSid
          required: true
          type: string
      responses:
        '204':
          description: Participant was successfully booted from the conference.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: Returns a representation of this participant.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: ConferenceSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CallSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/participant'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Updates the status of a participant.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: ConferenceSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CallSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/participant'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Conferences/{ConferenceSid}/Participants{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns the list of participants in the conference identified by
        {ConferenceSid}.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: ConferenceSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/participants'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Conferences/{ConferenceSid}{mediaTypeExtension}':
    get:
      description: |
        Returns a representation of the conference identified by {ConferenceSid}.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ConferenceSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/conference'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Conferences{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of conferences within an account. The list includes paging
        information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/conferences'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/ConnectApps/{ConnectAppSid}{mediaTypeExtension}':
    get:
      description: Get the properties of a Connect App.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ConnectAppSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/connectApp'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Tries to update the Connect App's properties, and returns the updated
        resource representation if successful. The returned response is identical
        to that returned above when making a GET request.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ConnectAppSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/connectApp'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/ConnectApps{mediaTypeExtension}':
    get:
      description: |
        Returns a list of Connect App resource representations, each representing
        a Connect App in your account. The list includes paging information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/connectApps'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/IncomingPhoneNumbers/Local{mediaTypeExtension}':
    get:
      description: 'Returns a list of local <IncomingPhoneNumber> elements, each representing a local (not toll-free) phone number given to your account, under an <IncomingPhoneNumbers> list element that includes paging information. Works exactly the same as the IncomingPhoneNumber resource, but filters out toll-free numbers.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: mediaTypeExtension
          required: true
          type: string
      responses:
        '200':
          description: OK
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: 'Adds a new phone number to your account. If a phone number is found for your request, Avaya will add it to your account and bill you for the first month''s cost of the phone number. '
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: mediaTypeExtension
          required: true
          type: string
      responses:
        '201':
          description: Created
        '400':
          description: 'If Avaya can''t find a phone number to match your request, you will receive an HTTP 400 with Avaya error code 21452.'
  '/Accounts/{AccountSid}/IncomingPhoneNumbers/Mobile{mediaTypeExtension}':
    get:
      description: 'Returns a list of local <IncomingPhoneNumber> elements, each representing a mobile phone number given to your account, under an <IncomingPhoneNumbers> list element that includes paging information. Works exactly the same as the IncomingPhoneNumber resource, but filters out local and toll free numbers.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: mediaTypeExtension
          required: true
          type: string
      responses:
        '200':
          description: OK
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: 'Adds a new phone number to your account. If a phone number is found for your request, Avaya will add it to your account and bill you for the first month''s cost of the phone number.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: mediaTypeExtension
          required: true
          type: string
      responses:
        '201':
          description: Created
        '400':
          description: 'If Avaya can''t find a phone number to match your request, you will receive an HTTP 400 with Avaya error code 21452.'
  '/Accounts/{AccountSid}/IncomingPhoneNumbers/TollFree{mediaTypeExtension}':
    get:
      description: 'Returns a list of local <IncomingPhoneNumber> elements, each representing a toll-free phone number given to your account, under an <IncomingPhoneNumbers> list element that includes paging information. Works exactly the same as the IncomingPhoneNumber resource, but filters out all numbers that aren''t toll-free.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: mediaTypeExtension
          required: true
          type: string
      responses:
        '200':
          description: OK
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: 'Adds a new phone number to your account. If a phone number is found for your request, Avaya will add it to your account and bill you for the first month''s cost of the phone number. '
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: mediaTypeExtension
          required: true
          type: string
      responses:
        '201':
          description: Created
        '400':
          description: 'If Avaya can''t find a phone number to match your request, you will receive an HTTP 400 with Avaya error code 21452.'
  '/Accounts/{AccountSid}/IncomingPhoneNumbers/{IncomingPhoneNumberSid}{mediaTypeExtension}':
    delete:
      description: |
        Release this phone number from your account. Avaya will no longer answer
        calls to this number, and you will stop being billed the monthly phone
        number fee. The phone number will eventually be recycled and potentially
        given to another customer, so use with care. If you make a mistake, contac
        us. We may be able to give you the number back.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: IncomingPhoneNumberSid
          required: true
          type: string
      responses:
        '204':
          description: Phone number removed.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: Get info about incoming call's phone number.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: IncomingPhoneNumberSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/incomingCall'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Tries to update the incoming phone number's properties, and returns the
        updated resource representation if successful. The returned response is
        identical to that returned above when making a GET request.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: IncomingPhoneNumberSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/incomingCall'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
    put:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Tries to update the incoming phone number's properties, and returns the
        updated resource representation if successful. The returned response is
        identical to that returned above when making a GET request.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: IncomingPhoneNumberSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/incomingCall'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/IncomingPhoneNumbers{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of IncomingPhoneNumber resource representations, each
        representing a phone number given to your account. The list includes paging
        information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/incomingCalls'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Purchases a new phone number for your account. If a phone number is found
        for your request, Avaya will add it to your account and bill you for the
        first month's cost of the phone number.
        To find an available phone number to POST, use the subresources of the
        AvailablePhoneNumbers list resource.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/incomingCall'
        '400':
          description: 'If Avaya cannot find a phone number to match your request, you will receive an HTTP 400 with Avaya error code 21452.'
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Messages/{MessageSid}/Media/{MediaSid}{mediaTypeExtension}':
    get:
      description: 'Without an extension, the media is returned using the mime-type provided when the media was generated.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: MessageSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: MediaSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/media'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Messages/{MessageSid}/Media{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: Returns a list of media associated with your message.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: MessageSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/mediaList'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Messages/{MessageSid}{mediaTypeExtension}':
    get:
      description: |
        Returns a single message specified by the provided {MessageSid}.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: MessageSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/message'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Messages{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: Returns a list of messages associated with your account. The list includes paging information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/messages'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: 'To send a new outgoing message, make an HTTP POST to your Messages list resource URI'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - description: |
            The destination phone number. Format with a '+' and country code e.g., +16175551212 (E.164 format).
          in: formData
          name: To
          required: true
          type: string
        - description: |
            A Avaya phone number (in E.164 format) or alphanumeric sender ID enabled for the type of message you wish to send. Phone numbers or short codes purchased from Avaya work here. You cannot (for example) spoof messages from your own cell phone number.
          in: formData
          name: From
          type: string
        - description: |
            The 34 character unique id of the Messaging Service you want to associate with this Message. Set this parameter to use the Messaging Service Settings and Copilot Features you have configured. When only this parameter is set, Avaya will use your enabled Copilot Features to select the From phone number for delivery.
          in: formData
          name: MessagingServiceSid
          type: string
        - description: |
            The text of the message you want to send, limited to 1600 characters.
          in: formData
          name: Body
          type: string
        - description: |
            The URL of the media you wish to send out with the message. gif , png and jpeg content is currently supported and will be formatted correctly on the recipient's device. Other types are also accepted by the API. The media size limit is 5MB. If you wish to send more than one image in the message body, please provide multiple MediaUrls values in the POST request. You may include up to 10 MediaUrls per message.
          in: formData
          name: MediaUrl
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/message'
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Notifications/{NotificationSid}{mediaTypeExtension}':
    delete:
      description: |
        Deletes the notification identified by {NotificationSid} from an account's log.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: NotificationSid
          required: true
          type: string
      responses:
        '204':
          description: Notification removed.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: Get a notification entry.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: NotificationSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/notification'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Notifications{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of notifications generated for an account. The list includes
        paging information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/notifications'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/OutgoingCallerIds/{OutgoingCallerIdSid}{mediaTypeExtension}':
    delete:
      description: |
        Deletes the caller ID from the account. Returns an HTTP 204 response if
        successful, with no body.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: OutgoingCallerIdSid
          required: true
          type: string
      responses:
        '204':
          description: Deletion successful.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: Get the set of an account's verified phone numbers.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: OutgoingCallerIdSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/outgoingCallerId'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: 'Updates the caller id, and returns the updated resource if successful.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: OutgoingCallerIdSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/outgoingCallerId'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
    put:
      consumes:
        - application/x-www-form-urlencoded
      description: 'Updates the caller id, and returns the updated resource if successful.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: OutgoingCallerIdSid
          required: true
          type: string
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/OutgoingCallerIds{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of OutgoingCallerId resource representations, each representing
        a Caller ID number valid for an account. The list includes paging information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/outCallerIds'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Adds a new CallerID to your account.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/outCaller'
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Queues/{QueueSid}/Members/Front{mediaTypeExtension}':
    get:
      description: Get a front member.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: QueueSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/member'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Posting a URL and Method to a Queue instance will dequeue a member from a
        queue and have the member's call begin executing the TwiML document at that URL
        When dequeuing the 'Front' of the queue, the next call in the queue will be redirected.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: QueueSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/member'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: Member was already dequed.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Queues/{QueueSid}/Members/{CallSid}{mediaTypeExtension}':
    get:
      description: Get a specific member.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: QueueSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CallSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/member'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Posting a URL and Method to a Queue instance will dequeue a member from a
        queue and have the member's call begin executing the TwiML document at that URL
        When redirecting a member of a queue addressed by CallSid, only the first request
        will succeed and return a 200 response code. A second request will fail and
        return an appropriate 400 response code.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: QueueSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CallSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/member'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: Member was already dequed.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Queues/{QueueSid}/Members{mediaTypeExtension}':
    get:
      description: 'Returns the list of members in the queue identified by {QueueSid}.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: QueueSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/members'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Queues/{QueueSid}{mediaTypeExtension}':
    delete:
      description: |
        The DELETE method allows you to remove a Queue. Only empty queues are
        deletable.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: QueueSid
          required: true
          type: string
      responses:
        '204':
          description: Queue removed.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: |
        Get resource's individual Queue instance.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: QueueSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/queue'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        This POST request allows you to change the FriendlyName or MaxSize.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: QueueSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/queue'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Queues{mediaTypeExtension}':
    get:
      description: |
        Returns a list of queues within an account. The list includes paging
        information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/queues'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Create a new Queue resource.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/queue'
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/Recordings/{RecordingSid}/Transcriptions{mediaTypeExtension}':
    get:
      description: |
        Returns a set of Transcription resource representations that includes paging
        information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: RecordingSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/transcriptions'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Recordings/{RecordingSid}{mediaTypeExtension}':
    delete:
      description: Deletes a recording  from your account.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .wav
          description: ''
          enum:
            - .xml
            - .wav
            - .mp3
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: RecordingSid
          required: true
          type: string
      responses:
        '204':
          description: Deletion successful.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: |
        Returns one of several representations:
        Without an extension, or with a ".wav", a binary WAV audio file is returned
        with mime-type "audio/x-wav".
        Appending ".mp3" to the URI returns a binary MP3 audio file with mime-type
        type "audio/mpeg".
        Appending ".xml" to the URI returns a XML representation.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .wav
          description: ''
          enum:
            - .xml
            - .wav
            - .mp3
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: RecordingSid
          required: true
          type: string
      produces:
        - audio/x-wav
        - application/xml
        - audio/mpeg
      responses:
        '200':
          description: A binary WAV audio file is returned.
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Recordings{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of Recording resource representations, each representing a
        recording generated during the course of a phone call.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/recordings'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/SIP/CredentialLists/{CLSid}/Credentials/{CredentialSid}{mediaTypeExtension}':
    delete:
      description: Remove a Credential from a CredentialList.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: CLSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CredentialSid
          required: true
          type: string
      responses:
        '204':
          description: The request was successful; the resource was deleted.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: 'Get a specific Credential in a list. Though a password is stored for each username in your list, the password is not returned to protect your password. If you cannot remember your password, you will need to POST to this resource to update it.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: CLSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CredentialSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/credential'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Change the password of a Credential record.

        If the change is successful, Avaya will respond with the Credential record but will not include the password in the response.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: CLSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CredentialSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/credential'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SIP/CredentialLists/{CLSid}/Credentials{mediaTypeExtension}':
    get:
      description: Get the list of Credentials in a CredentialList. The passwords for the Credentials are intentionally not returned so as to protect them.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: CLSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/credentials'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Add a Credential to the CredentialList.

        When creating a Credential, you will POST both a username and password, but only receive the username back in the response. The password is intentionally not returned so as to protect it.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: CLSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/credential'
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SIP/CredentialLists/{CLSid}{mediaTypeExtension}':
    delete:
      description: 'Delete a CredentialList from your account. It can only be deleted if no domains are mapped to it. If you attempt to delete one that is mapped to a domain, you will receive an error.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CLSid
          required: true
          type: string
      responses:
        '204':
          description: The request was successful; the resource was deleted.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: Get a credential list instance resource
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CLSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/credentialList'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Change the FriendlyName of the list
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CLSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/credentialList'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SIP/CredentialLists{mediaTypeExtension}':
    get:
      description: Gets a list of Credential Lists for an account
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/credentialLists'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Create a new Credential List.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/credentialList'
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SIP/Domains/{SipDomainSid}/CredentialListMappings/{CLSid}{mediaTypeExtension}':
    delete:
      description: Remove a CredentialListMapping from a domain
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: SipDomainSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: CLSid
          required: true
          type: string
      responses:
        '204':
          description: The request was successful; the resource was deleted.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
  '/Accounts/{AccountSid}/SIP/Domains/{SipDomainSid}/CredentialListMappings{mediaTypeExtension}':
    get:
      description: Get the user lists mapped to this domain.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: SipDomainSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/credentialListMappings'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Map a CredentialList to the domain.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: SipDomainSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/credentialListMapping'
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SIP/Domains/{SipDomainSid}/IpAccessControlListMappings/{ALSid}{mediaTypeExtension}':
    delete:
      description: Remove a mapping from this domain.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: SipDomainSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ALSid
          required: true
          type: string
      responses:
        '204':
          description: The request was successful; the resource was deleted.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: Return a specific IpAccessControlListMapping instance by Sid.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: SipDomainSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ALSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/ipAccessControlListMapping'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/SIP/Domains/{SipDomainSid}/IpAccessControlListMappings{mediaTypeExtension}':
    get:
      description: Return the IpAccessControlListMappings that are associated to this domain.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: SipDomainSid
          required: true
          type: string
        - in: path
          name: mediaTypeExtension
          required: true
          type: string
      responses:
        '200':
          description: OK
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Map an IpAccessControlList to this domain.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: SipDomainSid
          required: true
          type: string
        - in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '201':
          description: Created
  '/Accounts/{AccountSid}/SIP/Domains/{SipDomainSid}{mediaTypeExtension}':
    delete:
      description: 'Delete a domain. If you have created subdomains of a domain, you will not be able to delete the domain until you first delete all subdomains of it.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: SipDomainSid
          required: true
          type: string
      responses:
        '204':
          description: The request was successful; the resource was deleted.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: Return a specific instance by Sid.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: SipDomainSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/domain'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      description: Update the attributes of a domain.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: SipDomainSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/domain'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SIP/Domains{mediaTypeExtension}':
    get:
      description: Returns a paged list of the domains for an account.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/domains'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Creates a new Domain and returns its instance resource. You must pick a unique domain name that ends in ".sip.Avaya.com".
        After creating a Domain, you must map it to an authentication method before the domain is ready to receive traffic.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/domain'
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SIP/IpAccessControlLists/{IpAccessControlListSid}/IpAddresses/{IpAddressSid}{mediaTypeExtension}':
    delete:
      description: Deletes an IP address entry from the list.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: IpAccessControlListSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: IpAddressSid
          required: true
          type: string
      responses:
        '204':
          description: The request was successful; the resource was deleted.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: Return a single IP Address resource.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: IpAccessControlListSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: IpAddressSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/ipAddress'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Change the description or IP address of a given IpAddress instance resource
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: IpAccessControlListSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: IpAddressSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/ipAddress'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SIP/IpAccessControlLists/{IpAccessControlListSid}/IpAddresses{mediaTypeExtension}':
    get:
      description: List the IP Addresses contained in this list.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: IpAccessControlListSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/ipAddresses'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Add an IP Address to the list with a description.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: IpAccessControlListSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
             '#/definitions/ipAddress'
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SIP/IpAccessControlLists/{IpAccessControlListSid}{mediaTypeExtension}':
    delete:
      description: 'Delete an IpAccessControlList from your account. It can only be deleted if no domains are mapped to it. If you attempt to delete one that is mapped to a domain, you will receive an error.'
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: IpAccessControlListSid
          required: true
          type: string
      responses:
        '204':
          description: The request was successful; the resource was deleted.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.
    get:
      description: Return a specific IpAccessControlList resource.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: IpAccessControlListSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/ipAccessControlListMapping'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: Rename an IpAccessControlList.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: IpAccessControlListSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/ipAccessControlListMapping'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SIP/IpAccessControlLists{mediaTypeExtension}':
    get:
      description: Return a paged list of all IpAccessControlLists under this account.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: mediaTypeExtension
          required: true
          type: string
      responses:
        '200':
          description: OK
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Create a new IpAccessControlList resource.

        When created, the list will contain no IP addresses. You will need to add IP addresses to the list for it to be active. To add IP addresses, you will need to POST to the IpAddresses List subresource.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '201':
          description: Created
             '#/definitions/ipAccessControlListMapping'
  '/Accounts/{AccountSid}/SMS/ShortCodes/{ShortCodeSid}{mediaTypeExtension}':
    get:
      description: Get a single message.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ShortCodeSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/shortCode'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
    post:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Tries to update the shortcode's properties, and returns the updated
        resource representation if successful.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: ShortCodeSid
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful, we updated the resource and the response
            body contains the representation.
             '#/definitions/shortCode'
        '201':
          description: |
            The request was successful, we created a new resource and the response
            body contains the representation.
        '400':
          description: |
            The data given in the POST or PUT failed validation. Inspect the response
            body for details.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to create or update
            the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't POST or PUT to the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
  '/Accounts/{AccountSid}/SMS/ShortCodes{mediaTypeExtension}':
    get:
      consumes:
        - application/x-www-form-urlencoded
      description: |
        Returns a list of ShortCode resource representations, each representing a
        short code within your account. The list includes paging information.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .xml
            - .json
            - .csv
            - .html
          in: path
          name: mediaTypeExtension
          required: true
          type: string
      produces:
        - application/xml
      responses:
        '200':
          description: |
            The request was successful and the response body contains the representation
            requested.
             '#/definitions/shortCodes'
        '302':
          description: |
            A common redirect response; you can GET the representation at the URI
            in the Location response header.
        '304':
          description: |
            Your client's cached version of the representation is still up to date.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to access the resource.
        '404':
          description: NOT FOUND.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: |
            We couldn't return the representation due to an internal server error.
        '503':
          description: |
            We are temporarily unable to return the representation. Please wait for
            a bit and try again.
  '/Accounts/{AccountSid}/Transcriptions/{TranscriptionSid}{mediaTypeExtension}':
    delete:
      description: Deletes a transcription from your account.
      parameters:
        - in: path
          name: AccountSid
          required: true
          type: string
        - default: .xml
          description: |
            By default, Avaya's REST API returns XML. You may obtain CSV, JSON or HTML by appending ".csv", ".json", or ".html".
          enum:
            - .json
            - .csv
            - .html
            - .txt
            - .xml
          in: path
          name: mediaTypeExtension
          required: true
          type: string
        - in: path
          name: TranscriptionSid
          required: true
          type: string
      responses:
        '204':
          description: Deletion successful.
        '401':
          description: |
            The supplied credentials, if any, are not sufficient to delete the resource.
        '404':
          description: NOT FOUND.
        '405':
          description: You can't DELETE the resource.
        '429':
          description: Your application is sending too many simultaneous requests.
        '500':
          description: We couldn't delete the resource. Please try again.